name: Build and Deploy .NET App

on:
  push:
    branches:
      - master   # triggers after PR merge

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore
      run: dotnet restore WebAPI_WebApp.sln

    - name: Build
      run: dotnet build WebAPI_WebApp.sln --configuration Release --no-restore

    - name: Test
      run: dotnet test WebAPI_WebApp.sln --configuration Release --no-build

    - name: Publish
      run: dotnet publish WebAPI_WebApp/WebAPI_WebApp.csproj -c Release -o ./publish --no-build

    - name: Deploy to Azure
      if: success()
      uses: azure/webapps-deploy@v2
      with:
        app-name: MyWebAPI1   # 🔴 replace this
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        package: ./publish
